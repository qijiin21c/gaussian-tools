# Gaussian skill

This project uses a dedicated `gaussian-runner` agent in `.claude/agent/`. The agent handles all Gaussian 16 workflow tasks including input preparation, job submission, and result analysis.
